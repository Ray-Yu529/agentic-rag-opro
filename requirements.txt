# Agentic RAG + OPRO optimizer — Demo dependencies
# 全部走 NVIDIA NIM API，本機不需要 GPU
openai>=1.30.0        # OpenAI 相容 client，指向 NVIDIA NIM
rank-bm25>=0.2.2      # sparse 檢索 (BM25)，純 Python
datasets>=2.18.0      # 載 HotpotQA
numpy>=1.24.0         # 向量運算 / cosine 相似度
python-dotenv>=1.0.0  # 讀 .env 的 NVIDIA_API_KEY
tqdm>=4.66.0          # 進度條
matplotlib>=3.7.0     # D5 對比曲線
optuna>=3.5.0         # (選用) 數值最佳化對照組，stretch goal
